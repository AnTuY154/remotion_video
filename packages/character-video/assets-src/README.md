# Legacy raster experiments

The files in this directory are historical raster-cutout experiments.

Production status:

- `doi-v2.webp.b64.*`: legacy V2 source
- `v3/`: incomplete experiment
- `v4/`: incomplete experiment
- `v5/`: incomplete experiment

Do not use these files as the master source for new production shots.

The current long-term direction is a GLB/GLTF skeletal character rendered with `@remotion/three`.

The old materializer is retained only so previous demos remain reproducible.
